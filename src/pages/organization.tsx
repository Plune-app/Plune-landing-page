import React from "react"

export const Organization : React.FC = () => {
  return (
    <div>
      {/* iterar listas de formularios disponiveis e ter no sidebar isto visível */}
      {/* ter sidebar para manipular os formulários cadastrados pelos users admins*/}
      {/* ter listagem de dados para cada página selecionada (paginas selecionadas sao formularios selecionados no sidebar)*/}
    </div>
  )
}